# Ausgabe der DICTS
```python
d.values() 
d.keys() #-> dict_keys(['key1', 'key2'])
d.items() #-> gibt Tupels als paare zurück dict_items([('key1', 'wert1'),('key2', 'wert2')])

```

# Ist enthalten?
```python
'mkey' in {'mkey'; 345} #--> TRUE
dict ={'mkey': 345}
345 in dict.keys()#--> FALSE
345 in dict.values()#--> TRUE
```

[[Dicts]]
 